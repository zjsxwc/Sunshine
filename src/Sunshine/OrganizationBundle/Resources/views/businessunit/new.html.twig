    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('admin_org_bu_index') }}">Back to the list</a>
        </li>
    </ul>

    <script>
        $(document).ready(function () {
            var companyName = $("#company-name").val();
            $("#sunshine_organizationbundle_businessunit_company").val(companyName);
        })
    </script>