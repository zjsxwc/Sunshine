    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <script>
        $(document).ready(function () {
            var companyName = $("#company-name").val();
            $("#sunshine_organizationbundle_businessunit_company").val(companyName);
            var node = $('#bu').tree('getSelected');
            var parentText = $('#bu').tree('getParent', node.target).text;

            if (companyName !== parentText) {
                $("#sunshine_organizationbundle_businessunit_parent").val(parentText);
            }
        })
    </script>