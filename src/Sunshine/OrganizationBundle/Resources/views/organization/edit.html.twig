{% extends "@SunshineOrganization/layout.html.twig" %}

{% block main_content %}
    {{ parent() }}

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="{{ 'sunshine.form.editSubmit'|trans }}" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('admin_org_front') }}">返回组织管理</a>
        </li>
    </ul>
{% endblock %}
